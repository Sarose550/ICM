# Backup files
**/*.bak

# Build artifacts
bench_grid
bench_avx2
bench_avx512
bench_topk
calibrate
calibrate_B
calibrate_dual
accuracy_bench
bench_amx
contour_1s
contour_1s_par
bench_gpu
bench_gpu_fused
calibrate_gpu
heatmap_gpu
push_limit_gpu
validate_planner_gpu
icm_debug_compare
micro_fft_vs_seq
build/

# Generated data (from calibration/benchmarking)
# fft_config.h / fftw_wisdom.dat are ignored at repo root (raw ./calibrate
# output before being copied to a device dir) but tracked under devices/*/
# — those are the hand-validated, committed source of truth per device.
calibration.csv
fft_config.h
fftw_wisdom.dat
!devices/*/fft_config.h
!devices/*/fftw_wisdom.dat
**/cpu_*.csv
**/*.png
# These are the repo's committed reference plots (generated by
# tools/plot_contour.py from results/ data) -- must be tracked.
!results/accuracy_convergence.png
!results/contour_1s.png
!results/parallel_speedup.png
!results/engine_dispatch.png
!results/runtime_vs_n_cpu.png
!results/runtime_vs_n_gpu.png
!results/gpu_contour.png
!results/contour_1s_m3pro.png
!results/parallel_speedup_m3pro.png
!results/engine_dispatch_m3pro.png
!results/runtime_vs_n_cpu_m3pro.png
zen4_*.txt
planner_validation.csv
gpu_limit_frontier.csv
results_b200_instance_*/
results_b200_local/
contour_*.csv
results_*.csv
gpu_heatmap.csv

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
*.so
*.dylib

# Editors
.vscode/
.idea/

# Claude Code
.claude/

# DeepSeek Deck worktrees (deck folder create --isolation worktree)
.deck-worktrees/

# Scratch investigation workspaces (not part of the shipped project)
scratch/

# OS
.DS_Store

# Zip archives
*.zip

# ── §7 Declutter: generated benchmark outputs ──────────────────
# Blanket patterns for throwaway output artifacts (regeneratable).
# Keepers live in results/ which is negated below.
*.csv
*.log
*.txt
bench_grid_*.txt
bench_*_b200.csv
bench_*_b200.log
gpu_*.csv
gpu_*.log
heatmap_*.csv
heatmap_*.txt
nk_*.csv
profile_*.csv
profile_*.txt
sample_plans*.csv
sample_plans*.log
accuracy_*.csv
vkfft_*.csv
vkfft_*.log
b200_*.csv
b200_*.txt
b200_*.log
results_b200_*/
results_b200_optimized/

# Compiled test binaries (leaked to repo root)
test_cpu_cost_model
test_cpu_cost_model_*

# Jupyter artifacts
.ipynb_checkpoints/

# ── Negation: results/ data files that back published numbers/plots ────
# (make results-refresh writes dated bench_grid_*/contour_*/gpu_heatmap_*
# files here; without these, new runs would be silently un-addable.)
!results/bench_grid_*.txt
!results/contour_*.csv
!results/gpu_heatmap_*.csv
!results/accuracy_convergence.csv
!results/b_optimal_sweep_*.csv
.dsar/

# AI agent project instructions -- not for public repo view
CLAUDE.md
